# セキュリティポリシー

Money:G - 完全オフライン家計簿アプリのセキュリティに関するポリシーと脆弱性報告の手順について説明します。ユーザーの金融データを外部送信ゼロで保護することが私たちの最優先事項です。

> **「外部送信ゼロ、完全オフライン家計簿」**
> 
> 金融データの安全性において、最も重要なのは「そもそもデータを外に出さない」ことです。このアプリは完全ローカル保存により、ネットワーク経由でのデータ漏洩リスクを根本的に排除します。

## 目次
- [サポートされているバージョン](#サポートされているバージョン)
- [セキュリティアーキテクチャ](#セキュリティアーキテクチャ)
- [脅威モデル](#脅威モデル)
- [脆弱性の報告](#脆弱性の報告)
- [セキュリティアップデート](#セキュリティアップデート)
- [データ保護](#データ保護)
- [セキュリティベストプラクティス](#セキュリティベストプラクティス)

## サポートされているバージョン

以下のバージョンに対してセキュリティアップデートを提供しています：

| バージョン | セキュリティサポート | 状態 |
| --------- | ------------------ | ---- |
| 1.3.x     | 完全サポート | アクティブ開発 |
| 1.2.x     | 重要な修正のみ | 限定サポート |
| 1.1.x     | 緊急修正のみ | レガシーサポート |
| 1.0.x     | サポート終了 | 終了 |
| < 1.0     | サポート終了 | 終了 |

### サポートポリシー
- **最新バージョン**: 完全なセキュリティサポートと機能更新
- **1つ前のメジャーバージョン**: 重要なセキュリティ修正のみ
- **2つ前のバージョン**: 緊急セキュリティ修正のみ
- **それ以前のバージョン**: サポート終了、アップグレードを強く推奨

## セキュリティアーキテクチャ

### 完全ローカル保存による根本的セキュリティ

このアプリケーションは「外部送信ゼロ」の設計思想により、従来のクラウドベースアプリとは根本的に異なるセキュリティアプローチを採用しています。

#### エアギャップ・アーキテクチャ
- **ネットワーク分離**: 金融データは一切ネットワークを経由しない
- **ローカル完結**: すべての処理をデバイス内で完結
- **外部依存性ゼロ**: インターネット接続なしでも完全動作
- **データ漏洩の物理的不可能性**: そもそもデータが外に出ない設計

### データ暗号化
- **ローカルデータベース**: SQLite暗号化実装（デバイス内完結）
- **機密データ**: AES-256暗号化でローカル保存
- **ファイルシステム**: デバイス固有の暗号化キー使用
- **デバイス固有キー**: AndroidKeystore/iOS Keychain活用

### 認証・認可
- **生体認証**: 指紋・顔認証対応（ローカル認証のみ）
- **PIN/パスワード**: デバイスローカルでの認証
- **セッション管理**: アプリレベルでの自動ロック機能
- **デバイスロック連携**: OS標準のセキュリティ機能活用
- **ハードウェアセキュリティ**: TEE（Trusted Execution Environment）利用

### データアクセス制御
- **最小権限の原則**: 必要最小限のデバイス権限のみ要求
- **完全ローカル**: データはデバイス内でのみ保存・処理
- **サンドボックス**: アプリ専用領域での厳格なデータ分離
- **外部アクセス遮断**: ネットワーク経由でのデータアクセス完全禁止
- **アプリ署名検証**: 改ざん防止とバイナリ整合性確認

### セキュリティ監査
- **定期監査**: 四半期ごとのセキュリティレビュー
- **依存関係チェック**: 自動脆弱性スキャン（dart pub audit）
- **コードレビュー**: すべてのセキュリティ関連変更の詳細レビュー
- **静的解析**: 継続的なコード品質とセキュリティチェック

## 脅威モデル

### 想定される脅威と対策

このアプリケーションの完全ローカル保存アーキテクチャに基づき、以下の脅威を想定し対策を実装しています。

#### 1. 物理デバイスアクセス脅威
**脅威**: デバイス紛失・盗難による第三者のデータアクセス
**対策**:
- デバイスレベルの暗号化必須
- 生体認証・PIN認証の多層防御
- AndroidKeystore/iOS Keychain活用
- 自動ロック機能

#### 2. マルウェア・悪意のあるアプリ
**脅威**: 他のアプリからのデータアクセス試行
**対策**:
- Androidアプリサンドボックス活用
- 最小権限の原則適用
- ファイル権限の厳格管理
- アプリ署名検証

#### 3. OS・プラットフォーム脆弱性
**脅威**: オペレーティングシステムの脆弱性悪用
**対策**:
- 最新OSへの対応
- プラットフォーム固有セキュリティ機能活用
- ハードウェアセキュリティモジュール利用

#### 4. アプリケーション改ざん
**脅威**: APK改ざん、リバースエンジニアリング
**対策**:
- コード難読化
- アプリ署名検証
- 実行時整合性チェック
- ルート化・脱獄検出

#### 5. 開発・配布プロセス脅威
**脅威**: 開発環境への侵入、配布チェーンへの攻撃
**対策**:
- GitHub Actions による自動ビルド
- 署名付きAPK/AABの配布
- 依存関係の脆弱性スキャン
- コードレビュー必須

### ネットワーク脅威の根本的排除

従来のクラウドベースアプリで問題となる以下の脅威は、本アプリでは根本的に排除されています：

- **ネットワーク盗聴**: データ送信自体が存在しない
- **サーバー侵害**: サーバーが存在しない
- **中間者攻撃**: ネットワーク通信が存在しない
- **DDoS攻撃**: オフライン動作のため無関係
- **APIセキュリティ**: APIが存在しない

## 脆弱性の報告

### 報告方法

**重要**: セキュリティ上の問題を発見した場合は、**公開のIssueトラッカーではなく**、以下の方法で報告してください。

#### 1. プライベート報告 (推奨)
```
GitHub Security Advisories:
https://github.com/paraccoli/flutter_finance_app/security/advisories/new
```

#### 2. 直接連絡
```
Email: GitHubのIssues経由でのプライベート連絡を推奨
Subject: [SECURITY] 脆弱性報告 - [簡潔な説明]
```

### 報告テンプレート

```markdown
## 脆弱性報告

### 概要
脆弱性の簡潔な説明

### 脆弱性の詳細
- **種類**: [ローカルデータアクセス / 暗号化回避 / 認証回避 / その他]
- **影響度**: [Critical / High / Medium / Low]
- **影響範囲**: [全ユーザー / 特定条件のユーザー / 物理アクセス必要]

### 再現手順
1. XXXを実行
2. YYYを入力
3. 脆弱性が発生

### 潜在的な影響
- ローカルデータへの不正アクセス
- 暗号化の回避
- 認証機構の迂回
- その他のリスク

### 推奨する修正方法
可能であれば修正案を提示

### 環境情報
- アプリバージョン: v1.3.4
- OS: Android 13 / iOS 16
- デバイス: [デバイス名]
- ルート化/脱獄: [有/無]

### 追加情報
スクリーンショット、ログ、POCコードなど
```

### 報告者への謝辞

セキュリティ脆弱性を報告していただいた方には：
- **謝辞**: READMEおよびリリースノートでの謝辞掲載
- **迅速対応**: 24時間以内の初回応答
- **進捗報告**: 修正プロセス全体の透明な情報共有
- **検証協力**: 修正版での検証協力のお願い

## セキュリティアップデート

### 対応プロセス

1. **受信・確認** (24時間以内)
   - 脆弱性報告の受信確認
   - 初期影響度評価
   - 報告者への確認連絡

2. **調査・評価** (72時間以内)
   - 詳細な技術調査
   - 影響範囲の特定
   - CVSSスコア算出
   - 再現性の確認

3. **修正開発** (影響度に応じて)
   - **Critical（クリティカル）**: 24-48時間以内
   - **High（高）**: 1週間以内
   - **Medium（中）**: 2週間以内
   - **Low（低）**: 次回定期リリース

4. **テスト・検証**
   - セキュリティテスト実施
   - 回帰テスト実行
   - 複数デバイスでの検証
   - 報告者による検証協力

5. **リリース・通知**
   - セキュリティパッチリリース
   - ユーザーへの適切な通知
   - CVE発行（必要に応じて）
   - セキュリティアドバイザリ公開

### 緊急対応プロセス

**Critical/High脆弱性**の場合：
- **即座対応**: 開発チーム緊急招集
- **事前通知**: 修正版リリース予告（GitHub Releases）
- **ホットフィックス**: 緊急パッチリリース
- **詳細報告**: 事後分析レポート公開
- **ユーザー通知**: アプリ内通知とGitHubでの告知

### 脆弱性の公開方針

- **修正完了後**: 詳細な脆弱性情報を公開
- **報告者への配慮**: 報告者の同意を得て謝辞を掲載
- **技術的詳細**: ユーザーの理解促進のための詳細説明
- **予防策**: 同様の脆弱性を防ぐための対策説明

## ローカル保存セキュリティの優位性

### アーキテクチャの革新的利点

Money:Gは**完全ローカル保存**アーキテクチャにより、従来のクラウドベースアプリとは根本的に異なるセキュリティレベルを実現しています。

#### ゼロトラストを超えた完全分離
- **データ外部送信ゼロ**: 金融データが物理的にインターネットに接続されない
- **エアギャップ効果**: ネットワーク経由の攻撃から根本的に保護
- **プライバシーバイデザイン**: 設計段階からプライバシー保護を最優先
- **攻撃面の最小化**: ネットワークAPIが存在しないため攻撃面が極小

#### デバイスネイティブセキュリティ活用
- **OS統合**: AndroidKeystore、iOS Keychain活用
- **ハードウェアセキュリティ**: TEE（Trusted Execution Environment）利用
- **セキュアエンクレーブ**: 暗号化キーのハードウェアレベル保護
- **プラットフォーム最適化**: 各OSの最新セキュリティ機能を最大活用

### 技術的実装詳細

#### デバイス固有暗号化
```dart
// デバイス固有暗号化キーの生成
class DeviceSecurityService {
  Future<String> generateDeviceKey() async {
    final deviceInfo = await DeviceInfoPlugin().androidInfo;
    final uniqueId = '${deviceInfo.id}_${deviceInfo.fingerprint}';
    return await _deriveKeyFromDevice(uniqueId);
  }
  
  // ハードウェアベースの暗号化
  Future<String> encryptWithHardware(String data) async {
    final keyAlias = 'money_g_key_${await getDeviceId()}';
    return await AndroidKeystore.encrypt(data, keyAlias);
  }
  
  // 暗号化データベース初期化
  Future<Database> initSecureDatabase() async {
    final dbPath = await getDatabasesPath();
    final deviceKey = await generateDeviceKey();
    return await openDatabase(
      join(dbPath, 'money_g_encrypted.db'),
      password: deviceKey, // SQLite暗号化
      version: 6,
    );
  }
}
```

#### マルチレイヤーセキュリティ
```dart
// 多層防御の実装
class SecurityManager {
  // Level 1: デバイス認証
  Future<bool> authenticateDevice() async {
    return await LocalAuthentication().authenticate(
      localizedReason: '金融データへのアクセス',
      biometricOnly: false,
    );
  }
  
  // Level 2: アプリ整合性検証
  Future<bool> verifyAppIntegrity() async {
    final packageInfo = await PackageInfo.fromPlatform();
    return await _verifySignature(packageInfo);
  }
  
  // Level 3: データ暗号化状態確認
  Future<bool> verifyDataEncryption() async {
    final db = await DatabaseService().database;
    return await _verifyEncryptionStatus(db);
  }
}
```

## データ保護

### 個人情報保護の徹底
- **完全ローカル**: 個人データは一切外部送信しない
- **デバイス内処理**: すべての計算・分析をローカルで完結実行
- **匿名化不要**: そもそも外部送信がないため匿名化処理も不要
- **データ主権**: ユーザーがデータを100%完全制御
- **第三者アクセス不可**: 開発者を含む誰もユーザーデータにアクセス不可

### 金融データ保護の革新性
- **エアギャップアーキテクチャ**: インターネット接続不要の完全独立動作
- **暗号化保存**: AES-256によるローカルデータベースの強力な暗号化
- **ハードウェアレベル保護**: デバイス固有キーによる暗号化
- **アクセス制御**: OS標準サンドボックスによる厳格な権限管理
- **監査ログ**: ローカルでのデータアクセス記録（外部送信一切なし）

### ローカル保存の圧倒的利点
- **プライバシー最優先**: データがデバイスから物理的に外に出ない
- **ネットワーク攻撃完全耐性**: オフライン動作でネットワーク脅威を根本回避
- **データ主権の完全実現**: ユーザーがデータを完全制御
- **規制準拠の自動達成**: 外部送信がないため多くの規制を自動クリア
- **政府要請への耐性**: データが存在しないため開示要求に応じることが不可能

### 準拠する規格・法令
- **個人情報保護法**: 日本の個人情報保護関連法令に完全準拠
- **GDPR**: EU一般データ保護規則（データ処理最小化原則を徹底実現）
- **CCPA**: カリフォルニア州消費者プライバシー法に準拠
- **PCI DSS**: 決済カード業界データセキュリティ基準（該当部分）
- **SOX法**: サーベンス・オクスリー法（財務データ保護要件）

### 法的優位性
- **データローカライゼーション**: 各国のデータローカライゼーション要件を自動満足
- **越境データ移転制限**: そもそもデータ移転が発生しないため制限対象外
- **政府アクセス要請**: 技術的にデータアクセスが不可能なため要請に応じられない
- **監査要求**: ローカルデータのため外部監査機関によるアクセスも不可能

## セキュリティベストプラクティス

### 開発者向けセキュリティガイドライン

#### セキュアコーディング標準
```dart
// 悪い例：平文でローカル保存
final sensitiveData = "user_financial_data";
await storage.write(key: "data", value: sensitiveData);

// 良い例：デバイス固有キーでの暗号化保存
final deviceKey = await DeviceSecurityService().generateDeviceKey();
final encryptedData = await EncryptionService().encryptWithDeviceKey(
  data: sensitiveData, 
  deviceKey: deviceKey
);
await SecureStorage().write(key: "encrypted_data", value: encryptedData);

// 最良例：ハードウェアセキュリティモジュール活用
final hardwareEncrypted = await HardwareSecurityModule().encrypt(
  data: sensitiveData,
  keyAlias: 'money_g_master_key'
);
await DatabaseService().saveEncrypted(hardwareEncrypted);
```

#### 依存関係セキュリティ管理
- **定期監査**: `dart pub audit`による脆弱性チェック（毎週実行）
- **最小依存**: 必要最小限の外部パッケージのみ使用
- **信頼性検証**: 使用パッケージの開発者・履歴の詳細確認
- **自動更新**: GitHub Dependabotによる自動セキュリティアップデート
- **ライセンス確認**: オープンソースライセンスの適切性確認

#### ローカルデータ保護実装
- **SQLite暗号化**: デバイス固有キーによるデータベース暗号化
- **ファイル権限**: アプリ専用ディレクトリでの0600権限設定
- **メモリ保護**: 機密データ使用後の即座なメモリクリア
- **デバッグ情報除去**: リリースビルドでのログ・デバッグ情報完全除去
- **コード難読化**: リバースエンジニアリング対策

#### セキュリティテスト実装
```dart
// セキュリティテストの例
class SecurityTest {
  // データベース暗号化テスト
  Future<void> testDatabaseEncryption() async {
    final db = await DatabaseService().database;
    final rawData = await _readRawDatabaseFile();
    assert(!rawData.contains('sensitive_pattern')); // 平文が存在しないことを確認
  }
  
  // 権限最小化テスト
  Future<void> testMinimalPermissions() async {
    final permissions = await _getAppPermissions();
    final expectedPermissions = ['WRITE_EXTERNAL_STORAGE']; // 必要最小限
    assert(permissions.length <= expectedPermissions.length);
  }
}
```

### ユーザー向けセキュリティガイド

#### アプリ利用時のセキュリティ強化
- **最新版必須**: セキュリティアップデートの即座適用
- **強力な認証**: 8桁以上のPIN、複雑なパスワード設定
- **生体認証推奨**: 指紋・顔・虹彩認証の積極活用
- **デバイス暗号化**: Android暗号化、iOS FileVault必須
- **物理的保護**: デバイス紛失・盗難の厳重な予防策

#### ローカルデータバックアップ戦略
- **定期エクスポート**: 週次でのCSVエクスポート実行
- **暗号化バックアップ**: エクスポートファイルの7zip暗号化
- **分散保管**: クラウドストレージと物理メディアの併用
- **復元テスト**: 月次でのバックアップ復元可能性確認
- **バージョン管理**: 複数時点でのバックアップ保持

#### インシデント対応手順
```
デバイス紛失・盗難時の緊急対応：

1. 即座実行（紛失確認から15分以内）
   - デバイスのリモートロック有効化
   - 「デバイスを探す」機能で位置確認
   - 通信キャリアへの回線停止依頼

2. セキュリティ確認（1時間以内）
   - リモートワイプ機能の検討・実行
   - 他のデバイスでのパスワード変更
   - 関連する金融機関への連絡

3. 復旧準備（24時間以内）
   - 新デバイスでのアプリ再インストール
   - 暗号化バックアップからのデータ復元
   - セキュリティ設定の再構築
```

#### 高度セキュリティオプション
- **デバイス専用化**: 金融管理専用デバイスの使用検討
- **エアギャップ運用**: インターネット接続を最小限に制限
- **物理セキュリティ**: 金庫・セーフティボックスでのデバイス保管
- **定期監査**: 月次でのデータ整合性・セキュリティ状況確認

## セキュリティメトリクス

### 公開実績指標
- **脆弱性対応時間**: 平均24時間（目標値：Critical 24h、High 72h）
- **セキュリティアップデート頻度**: 月次定期 + 緊急時即座
- **侵害報告**: 過去実績0件（完全ローカル保存による根本的予防）
- **ダウンタイム**: 0秒（オフライン動作のため）

### アプリ内セキュリティ監視
- **ローカル異常検知**: 不正なファイルアクセスパターンの検出
- **認証失敗監視**: 連続した認証失敗の記録と対策
- **データ整合性**: ローカルデータベースの定期整合性チェック
- **アプリ改ざん検知**: バイナリファイルの署名・ハッシュ値検証
- **デバイス状態監視**: ルート化・脱獄デバイスの検出と警告

### セキュリティ品質保証
- **コードカバレッジ**: セキュリティ関連コードの100%テストカバレッジ
- **静的解析**: SonarQube、Semgrep等による継続的コード解析
- **依存関係監査**: 週次での`dart pub audit`実行
- **ペネトレーションテスト**: 四半期ごとの外部セキュリティ監査

## インシデント対応

### インシデント分類・対応時間
- **P0 - Critical**: ローカルデータ漏洩、暗号化破綻（4時間以内対応）
- **P1 - High**: 認証回避、権限昇格（24時間以内対応）
- **P2 - Medium**: 限定的機能影響、軽微な情報漏洩（1週間以内対応）
- **P3 - Low**: UI脆弱性、軽微なセキュリティ問題（次回リリース）

### 緊急対応体制
- **24時間監視**: GitHub Issues、Security Advisoriesの常時監視
- **緊急連絡網**: 開発チーム、セキュリティ専門家への即座連絡
- **ホットフィックス**: 緊急時48時間以内のパッチリリース
- **ユーザー通知**: アプリ内通知、GitHub、SNSでの迅速な情報提供

## 連絡先・報告

### セキュリティ関連連絡先
- **GitHub Issues**: [flutter_finance_app/issues](https://github.com/paraccoli/flutter_finance_app/issues)
- **GitHub Security**: [Security Advisories](https://github.com/paraccoli/flutter_finance_app/security/advisories)
- **開発者Twitter**: [@paraccoli](https://twitter.com/paraccoli)

### 報告確認プロセス
- **初回応答**: 24時間以内の受信確認
- **詳細確認**: 72時間以内の詳細回答
- **進捗報告**: 修正完了まで定期的な進捗共有
- **完了通知**: 修正完了とリリース情報の通知

### 謝辞・貢献者
セキュリティ向上にご協力いただいた方々への感謝を込めて、適切な謝辞を提供いたします：
- README.mdでの貢献者リスト掲載
- リリースノートでの特別謝辞
- セキュリティホールオブフェイムでの永続的な記録

---

**最終更新**: 2025年11月12日  
**次回見直し予定**: 2025年2月12日（四半期ごと更新）  
**ポリシーバージョン**: 2.0  

**作成者**: Paraccoli  
**承認者**: Money:G開発チーム  

---

**注意**: このセキュリティポリシーは、Money:Gアプリの「完全ローカル保存・外部送信ゼロ」という革新的なアーキテクチャに基づいて設計されています。従来のクラウドベースアプリケーションとは根本的に異なるセキュリティアプローチを採用しており、ネットワーク経由での攻撃リスクを物理的に排除しています。
